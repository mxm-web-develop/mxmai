---
description: 禁止在用户 UI 暴露系统内部设计；知识卡引用按任务形态选交互（纯选卡 vs 混合语气）
alwaysApply: true
---

# UI：禁止暴露内部设计 / 知识引用交互

## 硬性禁止

- **禁止**把 schema 内部条件、字段名、管道参数直接展示给终端用户，例如：
  - `style=其他时可选`
  - `writing_folder_id` / `style_custom` / `x-show-when` / `x-asset-source`
  - `folder_param`、`inject_as_param`、`taskKey`、`subtype`、路由键
- **禁止**对「知识库 / 语感文风 / 视觉风格 / 角色 / 知识卡」引用只丢一个**空白独立输入框**，再塞一长段技术说明期望用户看懂后手填 ID 或乱写。
- Admin / 调试面板可以保留技术字段；**面向创作引导、SchemaForm 用户态、对话式填表**必须用人话。
- **禁止**把上游 Provider / 网络原始错误（含 `proxy=`、内部 URL、`request_id`、完整 JSON body、堆栈）直接展示给非 admin 用户；须走平台错误码与产品文案（见 `docs/adr/user-facing-error-policy.md`）。

## 知识卡 / 文件夹卡引用（必须）

按任务形态选交互，**禁止**给纯选卡场景塞「输入框 + @」：

### 纯选卡（`mode=pick` / `withText=false`）

适用于：话题写作「我的文风卡」、SchemaForm `folderCard` 等**只能选卡、不能手填文风**的场景。

1. **推荐 chips**：列出就绪卡，一点即选（与内置风格 chips 同语言）。
2. **搜索过滤**：卡较多时用搜索框过滤列表；搜索只做过滤，**不是**「输入文风」。
3. **禁止**自由文本框、**禁止** `@` 按钮 / 「输入 @ 选择…」占位。
4. 选中后用可读标签展示卡名，提交时写回真正的 `*_folder_id`。
5. 空态用人话引导去知识库创建/等解析，不暴露内部字段名。

### 混合语气（`mode=compose` / `withText=true`）

适用于：行业日报「其他」等**可写一句语气偏好，并可选挂卡**的场景。

1. **推荐语气** chips + 文本框。
2. **@ 联想**：输入框支持 `@` 过滤并选择对应 `card_tag` 的卡（系统卡 + 用户卡）。
3. 选中后用 `@卡名` 展示，提交时写回 `*_folder_id`（可与文本并存）。

实现参考：`web/src/components/knowledge-base/FolderCardAtField.tsx`；文案过滤：`web/src/lib/uiCopyHygiene.ts`。

## 文案

- `title` / `description` / placeholder / help：只写用户任务语言（「选一种语气」「可参考已有文风」）。
- 条件显隐、注入逻辑写在 `x-show-when` / `x-asset-source` 等**机器字段**里，**不要**复述进 `description`。
- 若 schema 里仍有内部说明，前端必须用 `userFacingCopy()` 过滤，不得原样渲染。

## 引导步

- `*_custom`、挂卡字段（`x-ui-type: folderCard` / `x-show-when`）**不得**单独成引导步。
- 挂在父枚举「自定义…」/「我的文风卡」展开区内联完成。
