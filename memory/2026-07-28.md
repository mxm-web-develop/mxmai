## Writing：Markdown 主存 + markdownToPdf sidecar

- 新 post 节点 `markdownToPdf`（默认 sidecar，Admin 可配 overwrite；失败不阻断，warnings/pdfRenderStatus）
- PDF 含封面+目录；预览读 `metadata.pdfStorage`，不做打开时现转
- 音视频引入仍优先 Markdown；旧 `renderDocumentPdf` 兼容别名，step 实现已删

## audio/group/multi-voice「多人语音」落地

- Bundle：`mxmcgi/src/tasks/examples/audio-group-multi-voice.business.json`（seed: `pnpm --filter mxmcgi run seed:audio-multi-voice`）
- 平台原语：`resolveDialogueTimeline` / `renderAudioTimeline`（adelay+amix 支持重叠）/ `dialogueLineTts`
- Warp：`audio-warp-pipeline` group 分支混音后 `__preRenderedAudio` 跳过单次 TTS
- C 端：`POST /api/v1/search/dialogue-content-scan` + `DialogueCastField` + Wizard 推荐 chips
- 人审：`syncDialogueLines` 回写 lines（保留 cue）；group 已开多角色 VoiceoverScriptEditor

## 部署状态变更：国内副机 Admin 停同步（H5 保留）

- **背景**：原双机架构 = 香港主力（8.218.14.129 全栈 + mxm-ai.com）+ 副机（8.136.186.242 Admin :8080 / H5 :80）
- **变更**：副机上的 **supermxmai Admin Web（:8080）停止同步**——意味着 `deploy:admin-china` 不再触发；副机 Admin 静态/反代香港那套被关/废弃
- **保留**：副机 **H5（:80，依旧 `pnpm deploy:h5-china`）**继续在线上，浏览器请求 `/api/*` 仍走 Next.js `rewrites` → 香港主力
- **生产主入口收敛**：仅香港 `https://mxm-ai.com`（境外用户 Admin / API）/ 副机 `http://8.136.186.242/`（H5）
- **影响**：
  - `pnpm deploy:admin-china` 命令保留但**不主动用**；相关 skill 已加状态注释
  - Admin 静态只在香港 `mxm-ai.com` 入口；大陆用户走 Admin 需经香港（之前副机 :8080 是国内入口）
  - 后续若大陆 Admin 入口再开，再单独评估（DNS / 边缘机房 / 仅 h5 反代）
- **未变**：香港主力 ssh = `mxm-hk`（Port 2222）；旧 IP `8.210.129.25` / `121.43.32.168` 仍废弃

## 多人语音审稿：单轴时间线 UI
- DialogueGuidanceTimelineReview：竖轴按 start/duration 绝对定位；重叠句贪心分列并排
- 点选块编辑；非气泡流水账

## 多人语音任务 fe6b1148 调试
- 根因1: dialogueLineTts 误用 Warp 文本模型 MiniMax-M3（报 sentence subtitles）→ 改用 speech-* / tts_model
- 根因2: normalizeClientAccessibleMediaUrl 把 localhost:3000 Gateway URL 误判 MinIO → bucket=api
- 任务已重跑完成 completed

## 多人语音成片不可播/无字幕
- 根因: completePreRenderedAudio 未写 storageInfo.bucket/keys → /media/audio 404
- 字幕: speech-2.8 无 API 字幕；改为 timeline merge → subtitle_data
- 已修补 fe6b1148；新任务走代码修复

## 多人语音成片间隔过大
- 根因: dialogueLineTts 把每句首尾停顿抬到 0.8s，成片按文件时长串排，抢词负 offset 被静音抵消
- 修复: 多人句不再抬停顿；resolveDialogueTimeline 按有效语音落轴
- 需重跑任务才再生混音
