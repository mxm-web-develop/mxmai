- Deer：`DeerProvider` 使用 `getDeerProviderKeys()`（DB 优先，无则 env），`withDeerKeyRotation` 在 429/限流时按 priority 切换下一条 key；已移除 `ensureClient` 里对 `process.env.DEERAPI_API_KEY` 的单独兜底，与 key 列表逻辑统一。
- Admin 物理模型连通性：`provider === deer` 且解析为 `image` 时走 `deer-image-connectivity-test.ts`（Flux：createFlux→轮询 getFluxResult；Seedream/Gemini：同步校验 URL/解析图像）；`resolveInferedModalityForConnectivityTest` 优先用 `provider_models`（同 model_key 多行）与 registry，避免 `scope=default` 误判为 text。
