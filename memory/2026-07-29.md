## Core Skill 垂直切片（开工落地）

- ADR: `docs/adr/core-skill-output.md`（含双平面 + `contract.assets`）
- Runtime: `mxmcgi/src/tasks/skill/*`；`warp-runner` output 走 skillPack；group 执笔取 SKILL+references；合同切片；text `task-engine` 认 skillPack
- **挂卡/参考图桥接**: `promoteParamsAssetsToContract` → `contract.assets.{cards,media,folders}`；skill 切片含 assets
- Admin: `AdminBusinessCoreSkillTab` 左树右编；切片业务 Tab 名 Core Skill / Skill；合同与 invoke-* 只读；`@` 自定义编辑器
- Bundle: 四 example 按规范重写；已 seed；`topic_count` 字符串 enum 已修 + 加载时 `sanitizeNumericEnumsInJsonSchema`
- 冒烟 / 单测: skill + promote-assets + form-param-normalize 通过
