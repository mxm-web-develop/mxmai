## Writing PDF 预览补强（2026-08-03）

- 长文 writing bundle 默认挂 post `markdownToPdf`（sidecar，封面+目录，`useLayoutLlm: false`）：industry-daily / topic-article；Seek 在 `polishManuscript` 之后追加
- 预览：`locateWritingCachedPdf` + `streamStorageObjectToResponse`（Range/206）；前端 pdf.js 带鉴权头 Range 加载，不再整包 blob
- 引入仍优先 `export?format=markdown`；简历保留 layout LLM
- 误 apply 的 marketing-plan / with-context / tech-intelligence 已从本地 DB 删除，相关 example bundle 与文档引用已清

## Bugfix：mxm-warp 丢 pdfStorage（同日晚）

- 现象：话题写作等 warp 任务预览仍是 Markdown；下载 PDF 按钮可另生成，但预览不走 sidecar stream
- 根因：`runPostPhase` 已跑 `markdownToPdf`，但 `writing-task.ts` 组装结果后直接 `break`，未把 `markdownToPdfStorage` 写入 `result.metadata.pdfStorage`
- 修复：`attachPdfSidecarToWritingResult`，warp 落库与非 warp post 共用；**已完成的旧任务需重新生成**才会带 sidecar
