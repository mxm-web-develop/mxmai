# 内地用户访问 mxm-ai.com（DNS 解析到 8.136.186.242）→ 跳转 Admin :8080
# 与 eshop-h5-china.conf（server_name _）并存；本文件域名更具体优先生效

server {
    listen 80;
    listen [::]:80;
    server_name mxm-ai.com www.mxm-ai.com;

    # 使用 $host 保留域名（解析到本机时等价于 IP:8080，URL 更友好）
    return 302 http://$host:8080$request_uri;
}
