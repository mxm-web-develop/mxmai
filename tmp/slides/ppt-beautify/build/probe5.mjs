import util from 'node:util';
import { FileBlob, PresentationFile } from '@oai/artifact-tool';
const p = await PresentationFile.importPptx(await FileBlob.load('/Users/mxm_pro/Desktop/晋升答辩模板-张瀚峰.pptx'));
const s = p.slides.items[0];
const sh = s.shapes.items[0];
console.log('shape name', sh.name, 'placeholderType', sh.placeholderType);
console.log('text inspect', util.inspect(sh.text, {depth:3, showHidden:true, maxArrayLength:20}));
console.log('text proto', Object.getOwnPropertyNames(Object.getPrototypeOf(sh.text)).slice(0,200));
console.log('paragraphs type', typeof sh.paragraphs, util.inspect(sh.paragraphs, {depth:3, showHidden:true, maxArrayLength:20}));
console.log('paragraphs proto', Object.getOwnPropertyNames(Object.getPrototypeOf(sh.paragraphs)).slice(0,200));
