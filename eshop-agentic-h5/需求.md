1- H5 围绕 Open API：女装 / 男装 / 童装 / 饰品四条商拍线，固定 3×3 宫格，可选 1～3 份并发生成（parallel_count）。
2- 「开始」页选业务 + 传图（相册/拍摄/历史资源，大图压缩）；「历史」查进度；完成后在详情点选宫格单格做 HD（`tools-hd`）：已裁格走单图（`is_grid=false`），整图联系表走宫格模式（`is_grid` + `grid_layout` + `grid_cell`）；提交前可确认可选画幅 `aspect_ratio`。
3- 文件归属仅 H5 本地：服务端存储不变；每次提交归入 `tasks/{jobId}/`（IndexedDB），完成后拉取平台 URL（含 metadata.gridCells 裁格）写入本地。